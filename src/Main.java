import java.util.ArrayList;
import java.util.Collection;
import java.util.Collections;
import java.util.Comparator;

public class Main {
    public static void main(String[] args) {
    Userinterface userinterface = new Userinterface();
    userinterface.menu();
    //Note: The raw CSV file is already sorted after age-group
        // When choosing the unsorted option, it might seem as the list is sorted after age-group, but is it not.
    }
}